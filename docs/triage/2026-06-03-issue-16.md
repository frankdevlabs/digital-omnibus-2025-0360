# Triage: issue #16 — Source health — 6 sources unreachable

- **Issue:** https://github.com/frankdevlabs/digital-omnibus-2025-0360/issues/16
- **Source:** source-health (multi-source)
- **Detected:** 2026-06-03
- **Type / playbook:** source-health / source-health

## What was flagged

Weekly source-health log filed 2026-06-03 listing six sources that returned
challenge pages or 4xx/5xx errors after retry:

| Code | Source | Reported status |
|---|---|---|
| T1-03 | EUR-Lex Cellar (CELEX 52025PC0837) | AWS WAF / HTTP 202 interstitial |
| T1-14 | Autoriteit Persoonsgegevens RSS | HTTP 503 (after retry) |
| T2-04 | EURACTIV tech feed | HTTP 403 (Cloudflare) |
| T2-05 | BEUC Digital Rights feed | HTTP 503 (after retry) |
| T2-07 | Bits of Freedom feed | HTTP 403 |
| T2-10 | Two Birds RSS (`watch.twobirds.com/rss/expertise`) | Defunct — 302 → homepage |

## Assessment

Cross-checked each source against `data/tracker-state.yaml` (updated by June 4 run)
and `~/law-tracker/prompt.md`. Each source falls into one of three categories:

**Transient / already resolved (no action needed):**

- **T1-03** — The Cellar endpoint uses a 303 redirect; the June 4 run successfully
  fetched and stored a hash. The cron prompt already documents the `-L` requirement.
  The watchlist note accurately describes the AWS WAF situation. No change.

- **T1-14** — Was 503 on June 3; re-baselined on June 4. The cron prompt now uses
  the canonical feed URL (`/feed/article/rss.xml`) with `-L` redirect-follow, which
  resolves the old `/nl/rss` redirect correctly. The watchlist URL is stale (old path).
  **Fix:** update watchlist URL to canonical.

- **T2-05** — Was 503 on June 3; re-baselined on June 4. Working. However, the
  watchlist note references a fallback URL (`…/rss/news-events`) that the cron prompt
  confirms now 404s. **Fix:** remove the incorrect fallback note.

- **T2-07** — Was 403 on June 3; re-baselined on June 4 after the cron was updated
  to follow the 301 redirect (`?feed=rss2` → `/feed/`). The watchlist URL is the old
  redirect form. **Fix:** update URL to canonical `/feed/` and sync the note.

**Persistent / permanent block:**

- **T2-04** — EURACTIV tech feed. The cron prompt documents: "every Euractiv feed
  variant currently returns a Cloudflare 'Just a moment…' JS challenge (HTTP 403) that
  curl cannot solve." The tracker state already marks it with a `# Cloudflare-blocked
  on every run; treat as unreachable` comment and stores no hash. This source has never
  been fetchable since tracking began and there is no working alternative URL. Keeping
  it in the watchlist and cron generates permanent source-health noise with no signal.
  **Fix:** remove T2-04 from `data/watchlist.yaml` and the cron prompt source table.

**Already removed / no action:**

- **T2-10** — Two Birds RSS was already removed from `data/watchlist.yaml` (no entry
  present) and from the cron prompt source table (not in Tier 2 list). The tracker
  state has no T2-10 entry. Fully cleaned up prior to this triage run.

## Plan

1. **`data/watchlist.yaml`** — three edits:
   - T1-14: update `url` from `https://www.autoriteitpersoonsgegevens.nl/nl/rss` to
     `https://www.autoriteitpersoonsgegevens.nl/feed/article/rss.xml`; update `notes`
     to match cron prompt (canonical URL; `/nl/rss` 302-redirects here; 5xx fallback:
     `/nl/actueel` HTML).
   - T2-04: remove entry entirely.
   - T2-05: update `notes` to remove the invalid fallback URL
     (`https://www.beuc.eu/rss/news-events` 404s per cron prompt).
   - T2-07: update `url` from `https://www.bitsoffreedom.nl/?feed=rss2` to
     `https://www.bitsoffreedom.nl/feed/`; update `notes` to state canonical URL.

2. **`~/law-tracker/prompt.md`** — two edits:
   - Remove T2-04 row from Tier 2 source table.
   - Update the Tier 2 section count from `(9)` to `(8)`.

3. **`data/tracker-state.yaml`** — no edit (auto-maintained; the stale T2-04 comment
   will be overwritten on the next cron run).

4. **`data/documents.yaml` / `sources/README.md`** — no change (none of the affected
   sources have a `document_id` requiring a register entry).

## Verification

- Run `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` — must report
  "0 broken".
- On next cron run: T2-04 should no longer appear in source-health summaries.
- T1-14, T2-05, T2-07 should produce hashes on next run (already baselined June 4).
- Close issue #16 once merged.
