# Triage: issue #138 — Source health: europarl WAF 14d auto-pause + OEIL 5xx

- **Issue:** https://github.com/frankdevlabs/digital-omnibus-2025-0360/issues/138
- **Source:** weekly source-health summary (`T1-01`, `T1-02`, `T1-04`, `T1-05`, `T1-06`, `T1-07`, `T1-20`, `T1-21`, `T1-22`)
- **Detected:** 2026-08-06T11:07:12Z
- **Type / playbook:** source-health / source-health

## What was flagged

Two unrelated outages, no genuine 0360 content hit this run:

1. **`www.europarl.europa.eu` AWS-WAF outage → auto-pause.** Every europarl-hosted
   source has returned the AWS-WAF JS challenge (HTTP 202 / ~2431-byte body; 0
   `es_document` blocks) on every run since 2026-07-24; last success 2026-07-23.
   2026-08-06 was the 14th consecutive failed day, so the routine set `paused: true`
   in `data/tracker-state.yaml` for **T1-04, T1-05, T1-06, T1-07, T1-20, T1-21, T1-22**.
   These are skipped on later runs until a human clears the flag. T1-05/T1-20/T1-21/T1-22
   are the high-value committee-document listings where new 0360 amendments / draft
   reports / opinions surface server-side.
2. **OEIL (oeil.europarl.europa.eu) 5xx.** T1-01 (HTML) and T1-02 (PDF) returned
   HTTP 500/000 — server-side 5xx, not a WAF challenge, different host. First failed
   day, not auto-paused. Side-effect: OEIL normally cross-covers the WAF-blocked EP
   committee sources, so this run the tracker had no view of EP committee activity from
   either channel.

## Assessment

Not a content change — a fetch problem. Live re-check on 2026-08-06:

| Source | Re-check (2026-08-06) | Verdict |
|---|---|---|
| T1-01 OEIL HTML | **HTTP 200** (115 KB) | **Recovered** — no action |
| T1-02 OEIL PDF | HTTP 500 (558-byte error page) | Still down; transient. T1-01 HTML is the authoritative fallback cross-cover |
| T1-04/05/06/07/20/21/22 (europarl) | HTTP 202, 0 bytes (plain curl) | WAF outage **ongoing** |

**Resume path confirmed viable.** The shared fetcher
`$HOME/law-tracker/lib/fetch_blocked_doc.py` (headless-Chromium tier, already used at
triage for doceo docs) **successfully retrieves the WAF-blocked listing pages** — tested
2026-08-06 on JURI/ITRE/LIBE/IMCO: exit 0, ~300 KB HTML, `es_document` blocks parse.
Piping its output to `ep_committee_docs.py <MNEMO> --stdin` reproduces per-doc detection.
So the sources can be resumed by wiring the fetcher into the daily fetch, without waiting
for the WAF to clear.

**Blackout completeness check (did the 14-day gap hide a 0360 committee doc?) — no.**
Fetched all four high-value listings and filtered to `(?i)digital omnibus(?! on ai)`:

| Committee | 0360 docs on listing | In repo? |
|---|---|---|
| JURI | PE789.142 (05-06 draft opinion), PE790.236 (29-06 amendments) | yes (`JURI-PA-789142`, `JURI-AM-790236`) |
| ITRE/LIBE | PE786.818 (22-06 joint draft report), PE791.883 (27-07 amendments) | yes (`CJ72-PR-786818`; `CJ72-AM-791883` in-flight via #133) |
| IMCO | PE789.877 (15-06 draft opinion), PE790.905 + PE791.061 (16-07 amendments) | opinion yes (`IMCO-PA-789877`); **amendments not yet in repo** |

The only blackout-window doc (PE791.883, 27-07) is already being integrated under issue
#133. Nothing new was lost to the outage.

### Side-finding (NOT part of this issue — do not bundle)

**IMCO tabled amendments PE790.905 (Am 125-328) and PE791.061 (Am 329-532), both dated
16-07-2026**, amend the IMCO draft opinion PE789.877 on file 2025/0360. They are **absent
from the repo and have no tracker issue.** They pre-date the WAF blackout (last europarl
success 2026-07-23), so this is a pre-existing detection gap, not a consequence of #138.
Per the one-issue rule this is left for a **separate `parliament-text` triage** (register
under `sources/parliament/`, transcribe via `transcribe-parliament-extract`, cascade to
IMCO cells). Flagged to the maintainer; not actioned here.

## Plan

Repo edits applied on this branch (`--fix`):

1. **`data/watchlist.yaml`** — record the fetch remediation in the single source of truth:
   - **T1-02**: add a note that the PDF-render endpoint is 5xx-prone (HTTP 500 on
     2026-08-06) and that the T1-01 HTML procedure-file is the authoritative fallback.
   - **T1-04 / T1-06 / T1-07**: add a note that the page WAF-blocks plain curl since
     2026-07-24 (HTTP 202; auto-paused, health #138) and must be fetched via
     `fetch_blocked_doc.py` before hashing.
   - **T1-05 / T1-20 / T1-21 / T1-22**: append to each existing note that the *listing
     page itself* is WAF-blocked, and that `fetch_blocked_doc.py <url> <tmp.html>` →
     `ep_committee_docs.py <MNEMO> --stdin` (confirmed 2026-08-06) resumes per-doc
     detection.

No `data/documents.yaml` / `sources/README.md` change (no document added or removed).

### Out-of-repo follow-up (human / cron owner — cannot be done from this repo)

- **`data/tracker-state.yaml` is auto-maintained ("Do not edit by hand")** — the
  `paused: true` flags are **left as-is**. Clear them only once the cron actually uses the
  fetcher for these sources (otherwise plain-curl polling will just re-fail and re-pause).
- Wire `fetch_blocked_doc.py` into `~/law-tracker/run.sh` for the seven europarl sources
  (and keep `~/law-tracker/prompt.md` in sync with the watchlist notes above), **then**
  clear the `paused` flags. Alternatively, if the WAF outage clears, verify plain curl
  returns 200 and clear the flags.
- Open a **separate** tracker issue / run `/tracker-issue` for the IMCO amendments
  PE790.905 + PE791.061 (side-finding above).
- OEIL T1-02: recheck next run; if it keeps 500ing, no action needed while T1-01 HTML is up.

## Verification

- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → "0 broken".
- `python3 -c "import yaml; yaml.safe_load(open('data/watchlist.yaml'))"` → parses.
- Fetcher resume path reproducible:
  `python3 $HOME/law-tracker/lib/fetch_blocked_doc.py "<europarl listing url>" /tmp/x.html`
  (exit 0) then `cat /tmp/x.html | python3 ~/law-tracker/lib/ep_committee_docs.py JURI --stdin`.
- Close #138 once the paused sources are resumed (out-of-repo step) and OEIL 5xx clears.
